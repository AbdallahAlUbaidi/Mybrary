<h2>Recently Added</h2>
<div>
    <%  books.forEach(book=>{ %> 
        <div><%= book.title %> </div>
        <img src="<%= book.coverImagePath %> " alt="<%= book.title %>" width="100" , height="150">
    <% })%>
</div>